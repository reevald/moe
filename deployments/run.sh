# If fresh installation
## Init langfuse (see: moe/worker/prompts/init_langfuse.sh for more info)
## Init migration and seeding (see moe/migrations/README.md for more info)

# Then run each docker compose file with project name equal to "svc-*"